package server

import "testing"

func TestDepartements(t *testing.T) {
	tt := map[string]struct {
		data map[string][]entry
	} {

	}

	for name, _ := range tt {
		t.Run(name, func(t *testing.T) {

		})
	}
}
